from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Measuring the effect of polling place closures on voter turnout',
    author='Tim Burke',
    license='',
)
